{
  "name": "techbook-template",
  "version": "1.0.3",
  "description": "Vivliostyle を使用した技術書自動組版テンプレート",
  "private": true,
  "scripts": {
    "build": "vivliostyle build && node scripts/add-line-numbers.js && vivliostyle build && node scripts/add-line-numbers.js --restore",
    "preview": "vivliostyle preview",
    "clean": "rimraf dist src/chapters/*.html"
  },
  "devDependencies": {
    "@vivliostyle/cli": "^8.0.0",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
